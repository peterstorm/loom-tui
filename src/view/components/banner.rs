use ratatui::{
    layout::Rect,
    Frame,
};

use crate::app::state::AppState;

/// Banner placeholder. Currently renders nothing.
pub fn render_banner(_frame: &mut Frame, _area: Rect, _state: &AppState) {
}
